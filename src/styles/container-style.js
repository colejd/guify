const csjs = require('csjs-inject');

import { theme } from '../theme.js';

module.exports = csjs`

.guify-container {
    position: absolute;
    left: 0;
    width: 100%;
    font-size: 11px;
}

`;
